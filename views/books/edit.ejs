<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <title>Editing <%= book.title %></title>
</head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Edit <%= book.title %> by <%= book.author %></h1>
    <form action="/users/<%= user._id %>/books/<%= book._id %>?_method=PUT" method="POST">
        <label for="title">Title:</label>
        <input type="text" name="title" id="title" value="<%= book.title %>">

        <label for="author">Author:</label>
        <input type="text" name="author" id="author" value="<%= book.author %>">

        <label for="finishedReading">Finished Reading?</label>
        <input type="checkbox" name="finishedReading" id="finishedReading" <% if (book.finishedReading) { %>checked<% } %>>

        <label for="recommend">Recommend?</label>
        <input type="checkbox" name="recommend" id="recommend" <% if (book.recommend) { %>checked<% } %>>

        <label for="notes">Notes:</label>
        <textarea name="notes" id="notes"><%= book.notes %></textarea>

        <button type="submit">Update Book</button>
    </form>
  </body>
</html>